{
  "name": "jobflip",
  "homepage": "https://christianschwer.github.io/jobflip/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -e client",
    "build": "react-scripts build"
  }
}
